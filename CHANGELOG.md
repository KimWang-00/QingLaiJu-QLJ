# 更新日志

## v2.3.3 (2026-08-19)

### 修复
- 优云智算 MiniMax H3 网关间歇性故障新增第二种形态的自动处理：遇 RetCode 230「Params [reference URL] not available」（素材 URL 校验失败，实测与图片传入形式无关、同一请求时好时坏）时，自动进入等待重试（每分钟一次、最长 30 分钟），期间交替使用图片 URL/压缩 base64 内嵌两种提交体，网关恢复后任务自动成功，无需手动重试；等待期间可随时手动停止

## v2.3.2 (2026-08-19)

### 优化
- AI 任务中心与进度条的失败状态标签改为单个红色图标（不再显示文字）
- 任务报错支持一键复制：任务中心报错区新增「📋 复制」按钮，进度条报错区新增复制图标，复制完整报错便于反馈排查

### 修复
- 视频模型（MiniMax H3）遇云端报图片 URL 不可用（如优云智算 RetCode 230「reference URL not available」）时，自动将图片压缩转 JPEG 后以 base64 内嵌重新提交，无需手动重试

## v2.3.1 (2026-08-19)

### 新增
- 局域网分享：应用设置 → 网络分享 页签，开启并重启后服务监听 0.0.0.0，同一局域网设备可通过 IP+端口（如 http://192.168.31.50:8000）访问 Web 面板；首次开启自动弹 UAC 配置防火墙入站规则，托盘菜单展示局域网地址

### 优化
- 项目文件夹化存储升级：无限画布/对话/工作流/3D 预演台等伴生数据全部随工程文件夹存储，整个项目文件夹可直接迁移备份
- 3D 组合模块（自定义物件阵列配方）改为项目级存储，切换项目自动载入对应配方，全局库作种子

### 修复
- 修复开启局域网分享后点「重启生效」弹「找不到文件」错误、新实例拉不起来的问题（含空格/中文路径下不再经 cmd/start 中转，改为延时接力重启）
- 修复托盘右键「退出/重启程序」在内置浏览器窗口打开时不生效的问题（退出时自动销毁浏览器窗口）

## v2.3.0 (2026-08-19)

### 新增
- 桌面托盘常驻：安装版启动后在系统托盘显示图标，右键菜单含 打开页面 / 开机自动启动开关 / 检查更新 / 重启程序 / 退出，并展示当前 Web 地址与版本号
- 内置轻量浏览器：基于 WebView2 内核的干净窗口（无地址栏/工具栏），程序启动后自动弹出并跳转到 Web 页面；关闭窗口后服务继续在托盘运行，可随时从托盘再次打开
- 无限画布超分组件：卡片内填写超分模型 API（地址/密钥/模型/×2~4 倍数），图片连入「输入图」一键超分；兼容 OpenAI 兼容 multipart 与 JSON base64 双协议，输出「高清图」可继续连给下游节点
- 智能助手侧边栏模式：助手面板 📌 按钮切换悬浮窗/侧边栏布局，侧边栏模式占据页面右侧独立栏位，可展示更多对话内容（偏好自动记忆）

### 优化
- 安装版不再弹出黑色控制台窗口（纯桌面应用体验）
- 重复启动防护：端口已被占用时不重复起服务，直接连接现有实例
- 画布生成任务接入任务中心重试（replay）：失败任务可用原参数重新提交

## v2.2.0 (2026-08-19)

### 新增
- 分镜首尾帧体系：生成尾帧前由 LLM 判断与前镜连贯性（连贯才接力前镜尾帧作参考，不连贯独立生成）；场景首镜/前镜无尾帧时自动先补首帧再生尾帧；分镜卡片自动轮播首尾两帧
- 编辑镜头弹窗：首尾帧本地上传 / AI 优化 / 删除；有首帧无尾帧时尾帧「⚡ 快速生成」（以本镜首帧+参考素材为参考）
- 镜头关联资产管理：编辑弹窗内添加/删除关联资产、为资产选择衍生版本（镜头级覆盖，生图/生视频参考同步生效）、上传本地参考素材并支持二次 AI 优化
- 全站图片涂抹优化组件：优化按钮统一弹出涂抹组件（画笔/橡皮/笔刷大小+文字要求），按原图分辨率合成标注图；模型支持多参考时「原图+涂抹图」局部修改，不支持时自动降级「原生成词+优化内容+涂抹图」
- 任务中心历史任务：完成/失败/停止的任务自动落盘，重启后仍可查看；面板新增「🕘 历史」视图按钮
- 报错任务「🔁 重试」：用原素材和资料重新执行；重启后的历史任务按落盘的重放参数重发原请求
- 首尾帧生图防漂移提示词：场景陈设/道具种类与位置/材质颜色严格忠实剧本描述，扩写仅允许补充摄影参数并追加固定硬约束

### 改进
- 镜头卡片左键单击即唤起操作菜单（排除复选框/放大/视频控件），双击直接编辑
- 图像接口瞬时网络故障自动重试（连接被拒/重置、超时、DNS、SSL 中断，最多 3 次间隔递增）
- 任务中心报错默认钳制 2 行，点击展开完整堆栈；重试/AI 分析按钮位于报错下方单独一行并排
- 尾帧生成一致性约束强化：不得增添/移除/更换家具道具，避免视频生成时画面跳变

### 修复
- 二级弹窗（涂抹优化/资产选择）垫在编辑弹窗遮罩下、误触关闭导致提交报 null 的问题（层级修正+提交防御）
- 帧卡按钮行换行问题（强制单行+弹窗加宽）
- Windows 后端控制台刷 ConnectionResetError 噪音（切换 selector 事件循环）
- SSL 连接中断被误报为「连接被拒绝」的文案问题

---

## v2.1.0 (2026-08-18)

### 新增
- 智能助手「完全访问模式」：开启后助手可直接读写项目/数据文件夹并执行命令
- 写操作逐条审批：拒绝 / 本次允许 / 本次会话允许 三选项
- 修改前自动备份，对话内修改记录支持一键回退（删除的文件也可恢复）
- 应用设置新增「提示词设置」标签页：自定义全部 13 类助手/流程提示词模板，支持保存生效与恢复默认
- 侧边栏左下角新增 GitHub 项目主页入口

### 改进
- 移除授权码（license）机制，开箱即用
- 助手指令体系更新：「模型设置」→「应用设置」，新增「提示词设置」导航指令
- 版本号全链路升级：侧边栏版本卡片、终端 banner、exe 与安装包命名同步 v2.1.0

---

## v2.0.0 (2026-08-17)

### 新增
- 全新品牌「轻来剧」，Q 造型 logo 设计
- 侧边栏折叠功能，支持 localStorage 持久化
- 版本检测卡片，自动检测服务器最新版本
- 无限画布模型能力过滤（按 capabilities.inputs 动态显示端口）
- 3D 预演台标注名称显示
- 终端启动 banner（ASCII 11 + 版权信息）
- 安装程序打包（Inno Setup 6）

### 改进
- 「模型设置」更名为「应用设置」
- 前端液态玻璃 UI 全面优化
- 侧边栏底部版本检测与项目切换布局优化

### 修复
- 3D 预演台黑球渲染问题
- 移动模式门控逻辑
- 选中框角标等比缩放

---

## v1.0.0 (初始版本)

### 核心功能
- 剧本工坊：AI 剧本生成
- 分镜中心：自动分镜管理
- 资产库：角色/场景/道具管理
- 无限画布：节点式 AI 工作流
- 3D 预演台：场景预览与机位设置
- 剪辑导演台：视频剪辑导出
- 智能助手：AI 对话与指令执行
